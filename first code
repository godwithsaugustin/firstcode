void main() {
  // my first code indeed

  String godwith = "test indeed";
  print(godwith);

  String qwerty = "qwerty indeed";
  print(qwerty);

  int godwith2 = 78;
  print(godwith2);

  int godwith3 = 3;
  print(godwith3);

          // string use double quote, int doesn't.
          // both are the same, one just does not use double quotes.
          // however use int for all mathematic operations.

  int a = 75;
  int b = 25;
  print(a + b);

  String c = "75";
  String d = "25";
  print(c + d);

    // there's the difference lmaooooo

          // let's try something other than addition hehe

  int q = 50;
  int w = 55;
  print(q * w);

            // there we go lmaooooooo
            // oh wait, we can use the previous ones. Let's see......

  print(a - b);

        // yeah we got it lmao, we can use the previous ones hahahaha

        // we found something new lol, numbers with points, int can't do it                   lmaoo.

  double test = 25.5;
  print(test);
  
           // BOOM, we got a decimal.
           // btw trying it with int doesn't work, it can't read decimals lmaoooo

  double r = 44.5;
  double t = 0.5;
  print(r + t);

  // let's goo, addition of decimals lmaooo. let's try some more

  double h = 64.3;
  double j = 0.7;
  print(h + j);
  print(h * j);
  print(h / j);
  print(h - j);

           // we doing decimal calculations fr lmaooo
           // just learned something, num can do both decimal and normal lmaooo

  num doofus = 25.99;
  print(doofus);
  num doofur = 24.01;
  print(doofur);

  // dope discovery fr lmaoooo

  print(doofus + doofur);
  print(doofus * doofur);

  // num rules fr lmaooo
